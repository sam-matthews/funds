-- s_price.tab
-- Sam Matthews
-- Create staging table for price data.
\! echo "======================="
\! echo "Creating table: S_PRICE"

CREATE TABLE IF NOT EXISTS s_price(
  sp_date DATE,
  sp_fund CHAR(10),
  sp_price REAL
);
